export enum ACCESS_PERMISSION {
  READ = "read",
  WRITE = "write",
  GET = "get",
  MANAGE =  "manage",
  UPDATE = "update",
  JOIN = "join",
  DELETE = "delete"
}

export enum RESOURCE_TYPE {
    CHANNEL = "channels",
    CHANNEL_GROUP = "groups",
    UUID = "uuids"
  }